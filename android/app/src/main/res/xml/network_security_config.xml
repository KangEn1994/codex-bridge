<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Linker encrypts the overlay network; Android must still permit its HTTP endpoint. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
